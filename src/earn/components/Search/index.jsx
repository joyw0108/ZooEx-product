import styles from './index.module.css';

const Search = () => {
  return (
    <div className={styles.container}>
      <div className={styles.title}>this is search bar</div>
    </div>
  );
};

export default Search;
